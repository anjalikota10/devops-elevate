import PageLayout from "../components/PageLayout";

export default function BeforeDevOps() {
  return (
    <PageLayout
      title="Before DevOps"
      description="Before DevOps, development and operations worked separately, leading to miscommunication, longer release cycles, and unstable production environments."
      nextPath="/"
      isLast
    />
  );
}
